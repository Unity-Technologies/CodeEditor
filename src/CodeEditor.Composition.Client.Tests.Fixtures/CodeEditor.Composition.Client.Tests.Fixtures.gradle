dependencies {
  'default' rootProject.references.nunitFramework
  'default' project(':CodeEditor.Composition')

  def rxReferences = [
    'System.Observable',
    'System.Reactive',
  ]
  rxReferences.each {
    'default' rootProject.references.rx(it)
  }
}
