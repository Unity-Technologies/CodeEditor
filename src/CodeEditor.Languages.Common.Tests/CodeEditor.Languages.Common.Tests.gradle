
dependencies {
  'default' rootProject.references.languagesCommon
	def projectReferences = [
		'CodeEditor.Features.NavigateTo.SourceSymbols.ServiceInterface',
		'CodeEditor.Grammars',
	]
	projectReferences.each {
		'default' project(":$it")
	}
}
