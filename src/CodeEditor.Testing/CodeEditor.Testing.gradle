
version = '0.1'

configurations {
	       tests
}

dependencies {
	tests project(path: ':CodeEditor.Composition', configuration: 'editor')
	tests rootProject.references.nunitFramework
	tests rootProject.references.moq
}
